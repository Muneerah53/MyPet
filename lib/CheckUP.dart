import 'package:flutter/material.dart';

class CheckUP extends StatelessWidget {
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('CheckUP page '),
      ),
    );
  }
}
